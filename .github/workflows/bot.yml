# DEZACTIVAT — botul rulează acum pe Fly.io (deploy-fly.yml)
# Dezcommentează și șterge liniile de mai jos dacă vrei să revii la GitHub Actions.
#
# name: 🤖 BotListă — 24/7 (fallback GitHub Actions)
# on:
#   schedule:
#     - cron: '0 */5 * * *'
#   workflow_dispatch:
# ...
